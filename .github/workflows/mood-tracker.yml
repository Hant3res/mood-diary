name: 💬 Дневник настроения

on:
  schedule:
    - cron: '0 18 * * *'
  workflow_dispatch:

permissions:  # ← ЭТО КЛЮЧЕВОЕ!
  contents: write  # Даём права на запись

jobs:
  track-mood:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}  # ← И это важно!
          
      - name: Update mood diary
        run: |
          FILE="mood_diary.txt"
          TODAY=$(date +%Y-%m-%d)
          
          if [ ! -f "$FILE" ]; then
            echo "ДНЕВНИК НАСТРОЕНИЯ" > $FILE
            echo "" >> $FILE
          fi
          
          echo "$TODAY: ______" >> $FILE
          
      - name: Show result
        run: cat mood_diary.txt
        
      - name: Commit and push
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git add .
          git commit -m "Добавлена запись настроения за $(date +%Y-%m-%d)"
          git push
