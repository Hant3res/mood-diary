name: 💬 Дневник настроения

on:
  schedule:
    - cron: '0 18 * * *'
  workflow_dispatch:

jobs:
  track-mood:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # Ключевое изменение!
          token: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Update mood diary
        run: |
          FILE="mood_diary.txt"
          TODAY=$(date +%Y-%m-%d)
          
          if [ ! -f "$FILE" ]; then
            echo "ДНЕВНИК НАСТРОЕНИЯ" > $FILE
            echo "" >> $FILE
          fi
          
          echo "$TODAY: ______" >> $FILE
          
      - name: Show result
        run: cat mood_diary.txt
        
      - name: Commit and push
        # Используем готовое действие для коммита
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Добавлена запись настроения за $(date +%Y-%m-%d)"
          file_pattern: mood_diary.txt
